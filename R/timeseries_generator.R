

#pseudocode

# abund.merge = data.frame(years = years,
#                          a = abund_generator(years))
# dat = expand_grid(doy = doy,
#                   years = years)
# dat = merge(dat, abund.merge)

